{
  "name": "bit-vector",
  "description": "Simple, fast bit vectors for javascript.  Useful in place of objects where performance is extremely important and your keys are all iteger convertible values)",
  "repository": "git://github.com/ashaffer/bit-vector.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "rm -rf lib && babel src --out-dir lib",
    "postpublish": "rm -rf lib"
  },
  "dependencies": {},
  "devDependencies": {
    "babel": "^5.8.21",
    "babel-tape-runner": "^1.2.0",
    "standard": "^5.1.0",
    "tape": "^4.2.0"
  }
}
